---
- project:
    merge-mode: squash-merge
    default-branch: main
    templates:
      - helpcenter-base-jobs-no-check
    vars:
      sphinx_pdf_files:
        - vbs-api-ref.pdf
        - vbs-umn.pdf
    check:
      jobs:
        - noop
    gate:
      jobs:
        - noop